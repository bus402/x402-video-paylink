{
  "name": "@x402-video/payment-receipt",
  "version": "1.0.0",
  "private": true,
  "description": "Payment Receipt types for x402 video streaming",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vitest": "^3.2.4"
  }
}
